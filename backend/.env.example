# Copie este arquivo para .env e preencha com seus valores locais.
# NUNCA commite o arquivo .env de verdade (ele já está no .gitignore).

# Porta em que o servidor vai rodar
PORT=

# String de conexão com o banco de dados
DATABASE_URL=

# Segredo usado para assinar tokens (se optarem por JWT)
JWT_SECRET=
